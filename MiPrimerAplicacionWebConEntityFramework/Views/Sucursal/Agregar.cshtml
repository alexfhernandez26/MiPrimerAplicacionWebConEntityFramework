@using MiPrimerAplicacionWebConEntityFramework.Models;
@model SucursalCLS
@{
    ViewBag.Title = "Agregar";
}

<h2>Agregar</h2>

@using (Html.BeginForm("Agregar", "Sucursal", FormMethod.Post, new {@id= "frmAgregar" }))
{
    <div class="row">
        <div class="col-md-6">
            <div>
                @Html.LabelFor(p => p.nombre)
                @Html.TextBoxFor(p => p.nombre, new { @class = "form-control" })
                @Html.ValidationMessage("nombre", new { @class = "text-danger" })
                @if (Model != null && Model.mensajeError != null)
                {
                     @Html.Label(Model.mensajeError, new { @class = "text-danger" })                
                }
            </div>
            <div>
                @Html.LabelFor(p => p.direccion)
                @Html.TextBoxFor(p => p.direccion, new { @class = "form-control" })
                @Html.ValidationMessage("direccion", new { @class = "text-danger" })
            </div>
            <div>
                @Html.LabelFor(p => p.telefono)
                @Html.TextBoxFor(p => p.telefono, new { @class = "form-control" })
                @Html.ValidationMessage("telefono", new { @class = "text-danger" })
            </div>
            <div>
                @Html.LabelFor(p => p.email)
                @Html.TextBoxFor(p => p.email, new { @class = "form-control" })
                @Html.ValidationMessage("email", new { @class = "text-danger" })
            </div>
            <div>
                @Html.LabelFor(p => p.fechaApertura)
                <!--Ojo para mostral la fecha es con editfor, pero el editfor por si solo, solo te muestra una-->
                <!--Caja de texto,para darle poder a ese editfor hay que usar los tag en la clase-->
                <!--[Datatype(Datatype.date)]-->
                <!-- new { htmlAttributes = new { class="form-control" } } Esto es para dar estilos al editfor de fecha-->
                @Html.EditorFor(p => p.fechaApertura, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessage("fechaApertura", new { @class = "text-danger" })
            </div>
            <div>
                <input type="submit" value="guardar" class="btn btn-primary" />
                @Html.ActionLink("Atras","Index","Sucursal", null,new {@class="btn btn-danger" })
            </div>
        </div>
    </div>
}

<script>
    var frmAgregar = document.getElementById("frmAgregar");
    frmAgregar.onsubmit = function (e) {
        e.preventDefault();
        if (confirm("Desea guardar cambios?") == 1) {
            frmAgregar.submit();
        }
    }
</script>